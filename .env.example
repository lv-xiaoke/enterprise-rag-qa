# ============================================================
# 环境变量模板
#
# 用法：复制成 .env 再填真实值
#     cp .env.example .env        # Windows: copy .env.example .env
#
# .env 已被 .gitignore 忽略（第 151 行），不会提交；
# 本文件会提交，所以**只放占位符，不要填真实 Key**。
#
# 代码在 model/factory.py 加载它，且不覆盖已存在的环境变量——
# shell / CI 里注入的值优先级更高。
# ============================================================

# --- 对话模型 + 评测裁判模型（默认 provider 为 deepseek）---
# 不填的后果：检索层评测、单元测试照常跑；Agent 问答与生成层评测会报
# 「DeepSeek 需要环境变量 DEEPSEEK_API_KEY」。
DEEPSEEK_API_KEY=

# --- 仅当把 provider 切成 dashscope 时才需要（见 config/rag.yml）---
# chat_provider : dashscope        -> 对话模型走通义千问
# embedding_provider : dashscope   -> 向量模型走 DashScope（默认 local，不需要 Key）
# DASHSCOPE_API_KEY=
