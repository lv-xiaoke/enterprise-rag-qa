# 字节跳动「后端开发实习生-中国交易与广告」面试准备稿

---

# 主项目：企业级智能知识库 Agent 系统

## 1. 一句话介绍（30 秒版）

> 我设计并实现了一个**企业级智能知识库 Agent 系统**，服务企业内部员工对制度流程、个人业务数据的查询需求。系统采用 ReAct 推理模式实现 LLM 对业务工具的自主编排调度，通过 RAG 管道将非结构化文档转化为可检索的知识，并落地为完整的 Streamlit Web 交互应用。我在其中负责了**整体架构设计、RAG 检索链路、Agent 工具调度、配置化管理和全链路异常兜底**。

---

## 2. 架构口述稿（2 分钟版）

> 整个系统的请求链路可以这样理解：

**请求入口层**：用户通过 Streamlit Web 前端发起自然语言查询（"我本周年假还剩几天？""报销流程是什么？"），前端负责会话状态管理、流式渲染和响应展示。

**接口/编排层**：查询到达 AgentExecutor，这是系统的核心编排引擎。它基于 LangChain 的 ReAct 框架，驱动 LLM 在"思考→行动→观察"的循环中自主决定调用哪个工具、传什么参数、何时结束。这一步本质上是一个**有限状态机**，每次循环做两件事：让 LLM 生成下一步操作计划；如果计划包含工具调用，就执行该工具并将结果反馈给 LLM 继续推理。

**工具/服务层**：Agent 可调用的工具被封装为独立的 Tool 对象，包括 RAG 知识检索、员工 ID 获取、部门查询、业务数据查询、周报上下文生成。每个工具都有明确的入参规范和异常返回约定，保证 LLM 即使传参有误也不会导致整个链路崩溃。

**数据存储层**：分为两条链路——（1）非结构化知识文档（PDF/TXT）经过 MD5 去重 → RecursiveCharacterTextSplitter 分片（200 字符/chunk，20 字符重叠）→ DashScope Embedding → Chroma 向量库持久化，检索时 Top-K=3 召回。（2）结构化员工数据（CSV）走懒加载 + 内存缓存策略，按 key 映射返回。

**中间件/横切层**：通过 middleware 机制实现了工具调用的日志监控和动态 prompt 切换——在周报生成场景下自动换用专用 prompt 模板，不影响主知识问答场景。

**监控与运维**：Logger 双通道（控制台 INFO + 文件 DEBUG），按日期滚动，关键节点（工具调用前后、模型调用前后）均有埋点。

---

## 3. 三个重点技术点（每个能讲 5 分钟）

### 技术点一：RAG 检索链路设计与质量保证

**为什么用 Chroma？**
Chroma 是 Python 原生的轻量级向量数据库，适合企业内网单机部署场景。对比 Milvus（需要额外服务进程）和 FAISS（无持久化能力），Chroma 在"开发体验、部署成本、持久化能力"三者间取得了最佳平衡。对于知识库规模在万级文档以内的内部系统，Chroma 完全够用。

**检索 Key 设计：**
我没有简单地直接用用户原文查询，而是将查询同时作为 embedding 输入和 retriever 的 invoke 参数，利用 LLM 对 query 的语义理解能力与 embedding 模型配合。Top-K=3 是基于企业制度文档的粒度（单篇通常 200-800 字）确定的：K 太小会丢失跨文档的相关信息，K 太大会引入噪声、增加 LLM 上下文负担。

**检索质量保证的四个纬度：**
1. **分片策略**：RecursiveCharacterTextSplitter 的分隔符优先级为 `["\n\n", "。", ".", "？", "?", "；", ";", "！", "!", " ", ""]`，优先在自然段落边界切割，逐级降级到句子、词、字符，保证每个 chunk 有独立的语义完整性，而非粗暴截断。
2. **重叠窗口**：chunk_overlap=20，确保跨 chunk 边界的上下文不会丢失，对于跨句查询尤其重要。
3. **文档去重**：MD5 哈希去重机制，文件以 4KB 分片流式读取（避免大文件 OOM），已入库文件的 MD5 持久化到 txt 文件，重启后不丢失。
4. **降级兜底**：prompt 中明确要求 LLM 在参考资料不足时输出"不确定，当前知识库没有覆盖该信息"，而非编造答案。

**如果再做一次，我会加入：**
- 检索质量离线评估集（标注一批 query → 预期文档对，定期跑 recall@k）
- 混合检索（BM25 稀疏 + embedding 稠密）互补召回
- Reranker 模型对 Top-K 粗排结果做精排

---

### 技术点二：ReAct Agent 工具调度与异常处理

**为什么用 ReAct 而不是固定流程？**
企业内部查询场景高度异质——用户可能问制度、查个人年假、生成周报、或混合查询（"我这个项目有什么风险？相关的报销流程是什么？"）。固定 if-else 路由无法覆盖组合意图。ReAct 让 LLM 作为"调度中心"，自主推理当前需要什么信息、调用哪个工具、拿到结果后是否需要进一步查询。这和后端微服务架构中的"API Gateway + 服务编排"思想一致。

**调度流程（以周报生成为例）：**
1. Thought：用户要我生成周报，我需要知道他的员工ID
2. Action：调用 get_employee_id（空入参）
3. Observation：返回 E1001
4. Thought：拿到ID了，接下来需要部门信息和周报上下文
5. Action：调用 get_employee_department + generate_weekly_report_context
6. Thought：我还需要拉取他本周的实际项目记录
7. Action：调用 fetch_employee_records("E1001,weekly_report")
8. Observation：返回完整的项目数据
9. Thought：信息齐全，可以生成最终周报
10. Final Answer：按四段式格式输出周报

**异常处理三层兜底：**
1. **解析层**：handle_parsing_errors 配置为中文提示，当 LLM 输出不符合 ReAct 格式时给用户可操作的反馈而非抛异常。
2. **运行时层**：工具调用通过 middleware 的 try-except 包裹，单个工具失败不会中断整个 Agent 循环。
3. **兜底层**：execute_stream 最外层 catch RuntimeError（格式反复失败）和通用 Exception，保证即使出现预期外错误，用户也能看到明确的系统提示而非白屏。

**工具调用的幂等性考虑：**
所有工具都是无副作用的读取操作（rag_summarize 检索、fetch_employee_records 查询 CSV），天然幂等。如果未来加入写操作（如提交请假申请），我会引入请求 ID 去重 + 数据库唯一约束。

---

### 技术点三：从单机 Demo 到可扩展系统的工程化设计

**配置管理：**
所有可变参数（模型名、chunk_size、Top-K、文档路径、提示词路径）全部抽到 YAML 配置文件中，通过 config_handler 统一加载为模块级常量。这样做的好处是：切换模型只需改 yml 不需要重新部署代码；不同环境（dev/staging/prod）可以用不同配置文件；运维可以直接修改配置而不需要理解 Python 代码。

**模型工厂模式：**
ChatModel 和 Embedding 都通过抽象工厂模式创建，如果公司后续要求切换到自部署的模型（如 Llama 3），只需要新增一个 Factory 实现类，业务代码零改动。

**日志与可观测性：**
- 双通道日志：控制台 INFO 级别（运维实时查看），文件 DEBUG 级别（按日期滚动存储，便于问题追溯）
- 关键链路埋点：工具调用前后（monitor_tool middleware 记录工具名 + 入参 + 成功/失败状态），模型调用前后（log_before_model 记录消息数和最新消息类型）
- 业务元数据：通过 config metadata 标记 business_scene，如果有 APM 系统可以直接关联

**流式输出设计：**
execute_stream 返回 Python generator，按 chunk 逐步 yield 输出。这个设计不是简单的"逐字打印"，而是为 SSE（Server-Sent Events）或 WebSocket 推送做好了准备——前端只需要消费这个 generator，不需要等待完整响应。

**会话持久化与导出：**
Streamlit session_state 在内存中维护完整对话历史，支持导出为 Markdown 文件（含时间戳、消息计数、引用来源），这本质上是一个轻量级的数据持久化方案——对于 demo 阶段够用，对于生产环境可以替换为 Redis + PostgreSQL。

---

## 4. 面试官追问版（7 个问题）

### Q1：你项目里最难的问题是什么？

**最难的是让 LLM 稳定遵循 ReAct 格式。**

具体来说，LLM 有时候会"跳过"工具调用直接编造答案（幻觉），有时候会生成格式不正确的 Action Input（比如 JSON 格式错误或缺少必要字段），有时候会在 Observation 之后重复之前已经执行过的 Action（死循环）。

我的解决方案分三层：
1. **Prompt 工程约束**：在系统 prompt 中明确写了"不编造制度条款、审批结果、报销状态"，并在 ReAct 模板的 Action Input 位置加了 `MUST NOT BE EMPTY` 的强约束。
2. **解析异常兜底**：AgentExecutor 的 handle_parsing_errors 没有用默认的英文报错，而是定制了中文提示告诉用户具体怎么调整问题。
3. **工具层防御**：工具函数内部对入参做了清洗（strip、replace 引号、校验分隔符），即使 LLM 传了脏数据也能容错处理。

---

### Q2：你做了什么优化？效果如何量化？

**文档入库链路优化：**

优化前：每次启动全量重新解析所有文档，embedding 重新计算，耗时随文档量线性增长。

优化后：引入 MD5 去重，文件按 4KB 分片流式计算哈希（避免大文件一次读入内存），已入库文件跳过。效果：知识库从 6 个文档增加到 N 个文档时，二次启动的入库耗时从 O(N) 降为 O(新文档数)，对于日常增量更新场景几乎零开销。

**检索延迟估算：**
当前未做压测，但可以估算：Chroma 本地持久化 + DashScope Embedding API（网络延迟 ~50-100ms）+ LLM 推理（qwen-max ~1-3s）。整体端到端延迟在 2-5 秒范围内。如果上线生产，我会先做三件事：① embedding 计算加本地缓存（相同 query 的 embedding 不重复请求 API）② Chroma 切换为服务化部署（Client-Server 模式）③ 在 Streamlit 前端做乐观 UI 更新（先显示"正在检索"，检索结果返回后逐字流式输出）。

---

### Q3：如果用户量扩大 10 倍，系统哪里先挂？

**第一个瓶颈是 Chroma 向量库的单机部署。**

当前 Chroma 使用本地持久化模式（PersistentClient），所有读写走本地磁盘，无法横向扩展。如果用户量从 10 人扩到 100 人，并发查询会把磁盘 IO 打满。

**我的扩展方案：**
1. Chroma 切换为 Client-Server 模式，独立部署为可水平扩展的检索服务
2. 在这一层前面加一个 Redis 缓存，热点 query（如"请假流程"）的 embedding 和检索结果直接缓存，命中率预估 30-40%（企业内部查询有长尾 + 热点并存的特性）
3. Embedding API 调用加连接池和超时控制，避免单个慢请求阻塞其他用户的查询

**第二个瓶颈是大模型 API 的调用频率限制。**
当前所有查询都走云端 qwen-max，高并发时会触发 API 限流。解决方案：① 引入请求队列削峰，② 对高频简单问题（如纯检索型问题）路由到 qwen-turbo，复杂推理才用 max，③ 积累足够数据后考虑离线微调小模型处理高频意图。

---

### Q4：如果向量数据库挂了怎么办？

**降级策略三级：**

1. **一级降级——提示用户**：向量库不可用时，Agent 仍然可以回答基于员工 CSV 数据的问题（年假、项目、报销），因为这些走的是结构化查询而非向量检索。同时给用户明确提示："知识库暂时不可用，当前仅支持个人业务数据查询"。

2. **二级降级——Agent 自我感知**：在 rag_summarize 工具内部，retriever.invoke 失败时返回特定错误信息（而非空列表），让 LLM 在 ReAct 推理中感知到"知识库不可用"，从而在 Final Answer 中诚实告知用户。

3. **三级恢复——快速重建**：Chroma 的持久化数据在本地磁盘的 chroma_db 目录，只要磁盘没坏，重启服务即可恢复。同时 load_document() 有 MD5 去重机制，重新入库不会重复计算已有文档的 embedding。如果是磁盘故障，原始文档在 data/ 目录有备份，重新跑一遍入库脚本即可恢复。

**当前未做但我认为应该做的：**
- Chroma 数据目录的定期备份（cron job 每日 rsync）
- 向量库健康检查端点（/health 返回 embedding 模型和 Chroma 连接状态）

---

### Q5：如果文档入库失败怎么办？

**入库失败的三种场景和处理：**

1. **文件解析失败**（PDF 损坏、编码错误）：get_file_documents 返回空列表 → 日志记录 WARNING → 跳过该文件 → 继续处理下一个。不会因为单个文件失败导致整批入库中断。

2. **Embedding 计算失败**（API 超时、网络抖动）：add_documents 是 Chroma 的批量操作，异常被外层 try-except 捕获 → 日志记录 ERROR + 完整堆栈（exc_info=True）→ 该文件的 MD5 不会被写入去重记录 → 下次启动时会重新尝试入库。

3. **MD5 计算失败**（文件被占用、权限不足）：get_file_md5_hex 返回 None → 跳过该文件 → 不阻塞其他文件。

**入库的"至少一次"语义：**
MD5 去重文件是追加写入（append mode），只在文档成功入库后才写 MD5。如果入库过程中系统崩溃，MD5 未写入，重启后会重新处理——这是"至少一次"语义，对企业场景是可接受的（文档不会丢，最多重复入库一次）。

---

### Q6：如何保证数据一致性？

**当前系统的数据一致性分两层：**

**知识库文档的一致性：**
- 原始文档（data/enterprise/）是 source of truth
- 向量库（chroma_db/）是 derived data，可通过 load_document() 从原始文档重建
- MD5 去重文件是处理状态的持久化记录
- 一致性保证：如果原始文档更新（内容变了 → MD5 变了 → 会被识别为新文档入库），旧版本的向量数据仍在 Chroma 中。当前未做旧版本清理，这是因为企业内部制度文档通常不会频繁修改，旧版本残留影响有限。

**员工业务数据的一致性：**
- CSV 文件是模拟数据源，每次请求时懒加载到内存字典
- 读操作无副作用，不存在写冲突
- 如果 CSV 文件被外部修改，下一次查询自动读到新数据（懒加载在首次调用时触发，之后缓存）

**如果重构我会做的事情：**
- 引入文档版本号，旧版本向量数据在下一次入库时标记过期并异步清理
- 对 Chroma 的写入操作加一个简单的文件锁（filelock），防止并发入库导致重复
- 将员工数据从 CSV + 内存字典迁移到 SQLite 或 PostgreSQL，支持事务性读写

---

### Q7：你这个项目有什么缺陷？如果重构会怎么做？

**当前缺陷（诚实的自我评估）：**

1. **没有检索质量评估体系**：我不知道 Top-K=3 的召回率到底是多少，不知道对于"请假流程"这样的查询，Chroma 返回的三个 chunk 是不是最相关的三个。需要建立标注数据集 + 定期跑 recall@k / MRR 指标。

2. **没有并发处理能力**：Chroma 本地模式 + Streamlit 单线程模型 + 内存字典缓存，当前是单用户设计。要支撑多用户需要引入异步框架（FastAPI + asyncio）、连接池、请求队列。

3. **工具调度缺乏熔断机制**：如果 LLM 陷入死循环（反复调用同一个工具），AgentExecutor 会一直运行直到 max_iterations。当前 max_iterations 用的是 LangChain 默认值，没有根据业务场景调优。

4. **缺少端到端测试**：只有模块级的 `if __name__ == '__main__'` 手动测试，没有自动化集成测试覆盖"用户提问 → 工具调用 → 返回答案"的完整链路。

**如果重构，我会做：**

| 层面 | 当前 | 重构后 |
|------|------|--------|
| 框架 | Streamlit 同步 | FastAPI + asyncio 异步后端 + 轻量前端 |
| 向量库 | Chroma 本地模式 | Chroma Client-Server / 自建 Milvus |
| 缓存 | 无 | Redis 缓存热点 query 的 embedding 和检索结果 |
| 任务队列 | 无 | Redis Queue / Celery 解耦入库和检索 |
| 数据库 | CSV 文件 | SQLite（当前规模）/ PostgreSQL（扩展后） |
| 监控 | 本地日志 | 结构化日志 + Prometheus metrics + 告警规则 |
| 测试 | 手动测试 | pytest 集成测试 + 检索质量离线评估 |
| 配置 | YAML 静态加载 | 配置中心热更新 + 灰度开关 |

**但我不会一开始就上全套微服务**，因为对于当前阶段（Demo 验证 + 面试展示），过度工程化反而掩盖了对问题本质的理解。正确的演进路径是：先用最简单的架构跑通核心链路 → 监控发现瓶颈 → 针对性优化。

---

## 5. 总结金句（30 秒收尾）

> 这个项目让我在**没有标准后端框架加持的情况下**，从零搭建了一个完整的 AI 应用系统。我关注的不只是"调通 API"，而是**检索质量、异常兜底、配置管理、链路可观测、扩展性预判**这些后端工程的基本面。如果加入字节的广告/交易团队，我有信心快速补齐 Go、微服务、高并发这些技术栈，因为我已经证明了：给我一个新领域，我能**用工程化的方式把它做出来并讲清楚为什么这样做**。

---

---

# 备选项目：基于数学建模的业务优化系统

## 1. 一句话介绍（30 秒版）

> 我在数学建模竞赛和科研中积累了**数据驱动的业务优化能力**，包括销售预测、动态定价、多目标资源分配和遗传算法求解。这些项目虽然不依赖传统的后端技术栈，但体现了将业务问题抽象为数学模型、设计求解策略、并转化为可执行方案的能力——这和广告投放中"预算分配优化、转化率预测、竞价策略调优"的底层思维高度一致。

---

## 2. 架构口述稿（2 分钟版）

> 我参与的数模项目一般遵循这样的流程：

**问题定义层**：将模糊的业务需求（"销量为什么下降了？""怎么定价利润最高？"）转化为可量化的数学问题。这一步最关键的能力是**抽象**——什么是指标？什么是约束？什么是目标函数？哪些变量之间有关系？

**数据层**：从多个数据源收集和清洗数据——销售记录、价格历史、竞品信息、季节性因素、促销活动标记等。用 Pandas 做数据清洗和特征工程，处理缺失值、异常值和多重共线性。

**建模与求解层**：根据问题类型选择合适的模型——时间序列预测（ARIMA/Prophet）、多目标线性规划（PuLP/CPLEX）、遗传算法（DEAP）、博弈论模型。这一步的核心是**算法选择和参数调优**，需要平衡求解精度和计算时间。

**验证与输出层**：通过留出法或交叉验证评估模型性能，输出可视化结果（matplotlib/seaborn），最终形成可操作的业务建议。

**这套方法论映射到广告/交易场景：**
- 销售预测 → 广告转化率预估、GMV 预测
- 动态定价 → 竞价策略优化
- 多目标优化 → 预算分配（ROI vs 曝光量 vs 转化数）
- 遗传算法 → 组合优化（选品、人群定向组合）

---

## 3. 三个重点技术点（每个能讲 5 分钟）

### 技术点一：销售预测与特征工程

**问题背景**：某零售企业需要预测未来 4 周的 SKU 级别销量，以指导库存管理和促销排期。

**特征工程设计：**
- 时间特征：星期几、是否周末、是否月初/月末、季度
- 滞后特征：前 1/2/4/8/12 周的销量（捕捉短期和中期趋势）
- 滚动统计：过去 4 周的均值、标准差（捕捉波动性）
- 外部特征：是否促销、节假日标记、竞品价格变化

**模型选择与对比：**
ARIMA 适合单序列预测但忽略外部因素；Prophet 处理季节性和节假日强但对短期突变不敏感；XGBoost 能把所有特征用起来但需要更多数据。最终选择了加权集成策略——Prophet 做基线趋势，XGBoost 捕捉外部因素影响。

**效果衡量：**
MAPE（平均绝对百分比误差）从纯 ARIMA 的 18% 降到集成模型的 11%。最关键的收益是促销期的预测准确率提升——从 45% 错误率降到 15%，这对库存决策影响巨大。

---

### 技术点二：动态定价的多目标优化

**问题背景**：酒店/机票/网约车场景，价格需要根据供需实时调整，同时考虑长期用户留存和短期收入最大化之间的平衡。

**建模思路：**
- 目标 1：收入最大化（价格 × 预期销量）
- 目标 2：用户留存率最大化（低价吸引复购）
- 约束：价格上下限、库存容量、竞品价格区间

**求解策略——遗传算法：**
用 DEAP 框架实现 NSGA-II 多目标遗传算法。染色体编码为各时段的价格向量，fitness 为（收入，留存率）的 Pareto 前沿。优势是可以在非凸、非连续的解空间中找到全局最优，劣势是计算量大。

**关键权衡取舍：**
在凌晨低需求时段，算法自然地给出了低价策略（拉留存）；在晚高峰时段给出了溢价策略（拉收入）。这符合"削峰填谷"的业务直觉，但算法是用数学方式自动发现的。

**工程化考虑：**
实际落地时我会把算法封装为定时任务（cron 每小时跑一次），结果写入 Redis 供前端直接读取（减少计算延迟），同时加入人工审核阈值——当价格变动超过 20% 时触发告警，需要运营确认。

---

### 技术点三：资源分配的多目标线性规划

**问题背景**：给定有限预算，如何在多个广告渠道间分配以最大化总 ROI，同时满足各渠道最低投放量约束。

**建模：**
- 决策变量：每个渠道的预算金额
- 目标函数：总预期转化数（基于历史 CVR 估算）
- 约束条件：总预算上限、每个渠道的 ROI 下限、品牌渠道的最低投放量

**求解工具选择：**
小规模（<10 个渠道）用 PuLP 做线性规划精确求解，大规模（>50 个渠道 + 时间维度）用遗传算法近似求解。关键洞察是：**业务中很多优化问题不需要全局最优解，"比人工好 + 计算可控"就够了。**

**与广告投放的关联：**
这个模型可以直接对应到广告预算分配场景：渠道 = 广告位/投放策略，转化数 = 订单/GMV，ROI 约束 = 每个策略的投入产出要求。如果加入实时反馈（CTR/CVR 随预算变化而衰减），模型从 LP 变为非线性规划，需要用在线学习（如 contextual bandit）持续调整。

---

## 4. 面试官追问版（7 个问题简答）

### Q1：最难的问题是什么？
**将模糊的"我们的定价有问题"转化为可求解的数学模型。** 业务方往往说不清目标函数和约束条件。我学会了先做探索性数据分析（分布、相关性、异常点），找出真正影响指标的变量，再定义优化问题。

### Q2：用什么指标量化效果？
预测类问题用 MAPE 和 RMSE，优化类问题用"优化后 vs 现状"的提升百分比。关键是不只看平均数，还要看分场景的表现（促销期 vs 非促销期、周末 vs 工作日）。

### Q3：如果数据量扩大 10 倍？
遗传算法的种群评估会变成瓶颈。解决方案：① 用 NumPy 向量化计算替代 Python for 循环（10-100x 加速），② 用 Ray/Dask 并行化种群评估，③ 引入早停机制（连续 N 代无改善即停止）。

### Q4：如果数据源断了？
历史数据离线备份在本地，模型降级为仅用历史均值做预测。同时输出置信区间（而不是单点预测），让业务方感知到不确定性。

### Q5：数据质量有问题怎么办？
建立数据质量检查脚本——缺失值比例、异常值检测（IQR/3-sigma）、分布偏移检测（KL 散度 vs 上周）。质量不达标时模型停止输出并告警。

### Q6：如何保证模型结果的可复现性？
所有随机种子固定，模型超参数和特征工程代码 Git 版本管理，输入数据按日期存储快照。

### Q7：有什么缺陷？
竞赛项目的最大问题是**缺乏线上闭环验证**——效果只在离线数据上评估，不知道真实业务环境中的表现。如果要在字节落地，我会推动 A/B 实验框架和在线指标监控。

---

## 5. 总结金句

> 数学建模训练让我形成了"**把业务问题抽象为可量化模型**"的思维习惯。广告和交易系统的核心——投放策略、预算分配、竞价优化——本质都是"在约束条件下求解最优化问题"。我的独特价值在于：既能把业务语言翻译成数学模型和代码，又能把算法结果翻译回可执行的业务决策。

---

---

# 附录：面试中可能需要的关键概念速查

## 如何把你的技术栈"翻译"成后端语言

| 你说的 | 面试官听到的 |
|--------|-------------|
| LangChain Agent | 服务编排引擎，LLM 驱动的动态路由 |
| ReAct 循环 | 有限状态机，推理-行动-观察循环 |
| Chroma 向量库 | 数据持久化层，向量索引服务 |
| MD5 去重 | 幂等性保证，增量同步 |
| YAML 配置 | 配置管理与热更新潜力 |
| middleware（工具监控） | AOP 横切关注点，可观测性 |
| generator 流式输出 | SSE/WebSocket 推送模式 |
| try-except 兜底 | 异常处理与降级策略 |
| ChatModelFactory | 工厂模式，依赖注入 |
| RecursiveCharacterTextSplitter | 数据处理管道，ETL |
| CSV 懒加载 | Lazy loading，按需加载 |
| Streamlit session | 会话状态管理 |

## 面试中的"不要"

- 不要只说"我用了 XX 框架"，要跟上为什么选它、放弃了什么
- 不要回避项目的局限性，坦诚 + 改进思路 > 假装完美
- 不要用"我调了个 API"轻描淡写，要说"系统在 XX 场景下通过 YY 机制保证了 ZZ"
- 不要陷入纯技术细节，始终关联到"这解决了什么业务问题"
- 不要背诵答案，要理解每个回答背后的逻辑，面试官会追问
